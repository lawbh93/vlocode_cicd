import Command from '../core/command';
/**
 * Command impl for force:org:shape:delete
 */
declare class OrgShapeDeleteCommand extends Command {
    private org;
    private shapeApi;
    constructor();
    /**
     * executes the delete command
     * @param context - the cli context
     * @returns {Promise}
     */
    execute(context: any): Promise<any>;
    /**
     * secondary validation for the cli.
     * @param context - the cli context.
     * @returns {Promise}
     */
    validate(context: any): Promise<any>;
    getHumanSuccessMessage(results: any): any;
}
export = OrgShapeDeleteCommand;
