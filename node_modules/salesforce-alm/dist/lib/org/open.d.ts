import { Org } from '@salesforce/core';
import { UX } from '@salesforce/command';
declare const open: {
    buildFrontdoorUrl(org: Org): Promise<string>;
    /**
     * gets a url for the workspace or test org
     * @param context - the cli context
     * @param force - the force api
     * @param orgApi - an org.
     * @returns {Promise}
     * @private
     */
    buildUrl: (org: Org, path: string, isSetup?: boolean) => Promise<string>;
    getDomainRetries: () => number;
    checkDns(domain: string): Promise<string>;
    delay(): Promise<void>;
    checkLightningDomain: (myDomain: string, ux: UX, retryCount?: number) => Promise<void>;
    _openBrowserWithUrl: (url: any) => Promise<void>;
    /**
     * either open user's desktop browser or just print the url in console.
     * @param context - the cli context
     * @param force - the force api
     * @returns {Promise}
     * @private
     */
    open: (org: Org, ux: UX, path: string, isSetup: boolean, urlOnly: boolean) => Promise<{
        url: string;
        orgId: string;
        username: string;
    }>;
};
export default open;
