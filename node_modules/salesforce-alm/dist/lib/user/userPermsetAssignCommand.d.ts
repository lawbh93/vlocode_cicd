import Command from '../core/command';
/**
 * Assign a named Permission Set to a list of users, a specific user, or the default user.
 */
export declare class UserPermsetAssignCommand extends Command {
    private org;
    private usernames;
    private readonly successes;
    private readonly failures;
    constructor();
    validate(context: any): Promise<any>;
    /**
     * executes the assign command
     * @param context - the cli context
     * @returns {Promise}
     */
    execute(context: any): Promise<any>;
    getHumanSuccessMessage(): void;
}
export default UserPermsetAssignCommand;
