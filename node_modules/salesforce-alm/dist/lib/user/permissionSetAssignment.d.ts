import User from './user';
export interface ReadablePermissionSet {
    id: string;
    _fields: object;
    _permSetName: string;
}
declare class PermissionSetAssignment {
    private org;
    private force;
    private logger;
    private _permSetName;
    private _fields;
    constructor(org: any);
    get id(): string;
    get isCreated(): boolean;
    get permSetName(): string;
    get fields(): any;
    getField(key: string): string;
    parsePermissionSetString(permSetString: string): {
        nsPrefix: any;
        permSetName: any;
    };
    create(user: User, permSetString: string): Promise<PermissionSetAssignment>;
    retrieve(psaId: string): Promise<this>;
    get(): ReadablePermissionSet;
}
export default PermissionSetAssignment;
