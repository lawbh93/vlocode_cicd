export declare class ApiLimitsCommand {
    execute(context: any): Promise<ApiLimit[]>;
}
/**
 * constructs ApiLimit objects from server response
 * exposed for unit testing
 * @param {Object} response
 * @returns {ApiLimit[]}
 */
export declare let parseResponse: (response: any) => ApiLimit[];
export interface ApiLimit {
    name: string;
    max: number;
    remaining: number;
}
