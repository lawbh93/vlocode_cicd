export declare class ForceIgnore {
    private pathToForceIgnoreFile;
    private parser;
    constructor(pathToForceIgnoreFile?: any);
    denies(file: string): boolean;
    accepts(file: string): boolean;
    private parseContents;
}
