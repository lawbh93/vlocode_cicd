export declare class MdapiCancelCommand {
    private stashKey;
    private org;
    constructor(stashKey?: string);
    /**
     * Executes the mdapi deploy cancel command
     * @param context - the cli context
     * @returns {Promise}
     */
    execute(context: any): Promise<import("./mdapiDeployCancel").MDDeployCancelReportOutput>;
    /**
     * Validates the mdapi deploy cancel command parameters
     * @param context - the cli context
     * @returns {Promise}
     */
    validate(context: any): Promise<any>;
}
