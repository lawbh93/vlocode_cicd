import Command from './command';
declare abstract class VarargsCommand extends Command {
    readonly validators?: Function[];
    keyValuePairs: any;
    constructor(name: string, validators?: Function[]);
    validate(context: any): Promise<any>;
}
export default VarargsCommand;
