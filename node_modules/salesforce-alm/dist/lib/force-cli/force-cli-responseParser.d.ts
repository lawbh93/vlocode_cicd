/// <reference types="xml2js" />
interface ExecuteAnonymousResult {
    compiled: boolean;
    compileProblem: string;
    success: boolean;
    line: number;
    column: number;
    exceptionMessage: string;
    exceptionStackTrace: string;
}
export declare let hasFaultString: (errMsg: string) => boolean;
export declare let getFaultString: (errMsg: string) => string;
/**
 * extracts the debug info header from the execute anonymous apex code request
 * @param {Object} responseBody - xml response to an executeAnonymous request
 * @return {string} the compiler's debug info
 */
export declare let getDebugInfo: (response: Object) => string;
export declare let getExecuteAnonymousResponse: (response: Object) => ExecuteAnonymousResult;
export {};
