"use strict";
/*
 * Copyright (c) 2018, salesforce.com, inc.
 * All rights reserved.
 * SPDX-License-Identifier: MIT
 * For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
const node_1 = require("../env/node");
const document_1 = require("../env/document");
// Helpful for tests running with jsdom
function getOwnerDocument(node) {
    const doc = node_1.ownerDocumentGetter.call(node);
    // if doc is null, it means `this` is actually a document instance
    return doc === null ? node : doc;
}
exports.getOwnerDocument = getOwnerDocument;
function getOwnerWindow(node) {
    const doc = getOwnerDocument(node);
    const win = document_1.defaultViewGetter.call(doc);
    if (win === null) {
        // this method should never be called with a node that is not part
        // of a qualifying connected node.
        throw new TypeError();
    }
    return win;
}
exports.getOwnerWindow = getOwnerWindow;
//# sourceMappingURL=utils.js.map