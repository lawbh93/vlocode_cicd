export declare class Uploader {
    private analytics;
    /**
     * Sends events from analytics to telemetry.
     */
    static upload(cacheDir: string, analyticsFilePath: string): Promise<void>;
    private constructor();
    /**
     * Reads the analytics events from file and sends them to the telemetry service.
     */
    private sendToTelemetry;
    private getOrgId;
}
